#include "main.h"

/**
 * main - a function that prints the alphabet, in lowercase, followed by a new line.
 *
 * Return 0
 */

char main(void)
{
  	char low;

	if (low = "a", low <= "z", low++)
	_putchar(low);
	_putchar('\n');

	return (0);

}
	
